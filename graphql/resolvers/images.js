module.exports = {
    Query: {
        async getImagesById(_, { id }) {
            return;
        }
    },
    Mutation: {
        async addImages(_, { images }) {
            return;
        }
    }
}